# Core API
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-multipart
python-dotenv
websockets

# AI / Vision (LIGHTER)
ultralytics==8.0.0

# Torch (CPU only, pinned)
torch==2.0.1+cpu
torchvision==0.15.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Vision
opencv-python-headless

# Scientific (minimized)
numpy
lapx

# REMOVE scipy unless absolutely required

# Utilities (optional)
colorama
